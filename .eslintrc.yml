env:
  es2021: true

extends: airbnb-base

parserOptions:
  ecmaVersion: latest


rules:
  no-unused-vars: 1
  no-console: 0
  import/extensions:
    - error
    - ignorePackages
    - js: always
  no-underscore-dangle: [2, { "allow": ["__filename", "__dirname"] }]